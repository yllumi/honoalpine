{
  "name": "honoalpine",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.tsx",
    "build:assets": "bun build.ts",
    "start": "bun run build:assets && NODE_ENV=production bun src/index.tsx"
  },
  "dependencies": {
    "hono": "^4.11.3",
    "mysql2": "^3.16.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}